电力塔放置逻辑（规范稿）

基本约束
- 放置坐标：塔中心落在网格交点（整数坐标）。占地范围为 x∈[xc-0.5, xc+0.5]，y∈[yc-0.5, yc+0.5]。
- 覆盖半径：棋盘距离 R = 6（含自身）。距离度量统一用棋盘距离。
- 塔间最小间距：
  - 若共线（同一条直线），曼哈顿距离 ≥ 3。
  - 若不共线，曼哈顿距离 ≥ 4。
- 占用冲突：不得与已有建筑/传送带/分拣器/喷涂器/塔占用的 1×1 范围重叠，也不得越出蓝图边界。

放置需求点
- 需供电点集合 S：本层所有建筑（含堆叠实验室的每层节点）、传送带节点、分拣器、喷涂器的占用格或中心点。
- 坐标系：以当前层占用包围盒左上为 (0,0)，仅处理该层；多层可按层独立，或定义垂直半径后跨层处理（见下）。

单层放置算法（贪心覆盖）
1) 生成候选点 P  
   - 扫描包围盒内所有空格（未被占用），对每个候选 p 计算覆盖集 C(p) = { s∈S | dist(p,s) ≤ R }。  
   - 可选优化：仅取需求点周围 R 范围内的空格，或先按步长 2～3 采样，再在最优附近 3×3 精扫。
2) 贪心选择  
   - U = S（未覆盖），T = ∅（已选塔）。  
   - while U 非空：  
     - 选能覆盖 U 中最多元素的候选 p*（|C(p*) ∩ U| 最大；并列优先“与已有塔距离更大”或“更靠中心”）。  
     - 校验 p*：不越界、不与占用冲突、与已有塔满足最小间距规则；不满足则选次优。  
     - 将 p* 加入 T，并从 U 移除 C(p*) ∩ U。
3) 输出  
   - 对 T 中每个点生成电力塔（z=当前层高度），写入 buildingArray/buildings，并更新 occupiedArea。

多层处理
- 独立模式：各层分别运行上述单层算法（仅平面距离）。  
- 跨层模式（暂时不支持跨层）：允许塔对上下层供电，定义垂直半径 Rz，并用 3D 距离（如 max(|dx|,|dy|) + k·|dz| ≤ R）。将多层需求点合并后同样贪心，生成的塔分配到对应层。

配置项建议
- 覆盖半径 R（默认 6）、距离度量（棋盘/曼哈顿）、是否跨层、垂直半径 Rz、候选采样步长、最小边界留空、塔间最小曼哈顿间距（共线/非共线）应作为 config 字段，统一驱动放置逻辑。
